{
  "name": "agent-router-cc",
  "description": "Plugins for deterministic, verified coding-task routing in Claude Code.",
  "owner": {
    "name": "MisterRaindrop"
  },
  "plugins": [
    {
      "name": "router",
      "source": "./",
      "description": "Routes coding subtasks to the cheapest capable model to save Opus tokens: the main session plans, reviews and verifies; the router CLI owns worktree isolation, supervision, concurrent dispatch, the verification queue, and environment-free gates on every diff."
    }
  ]
}
