{
  "name": "router",
  "version": "0.8.0",
  "description": "Routes coding subtasks to the cheapest capable model to save Opus tokens. The main session plans, reviews the whole diff and owns the pass/fail verdict; the router CLI owns the mechanism -- isolated worktrees, process supervision, concurrent dispatch, an exclusive-lock verification queue for projects whose build environment exists only once, and environment-free gates on every diff (scope, secrets, executable bit).",
  "author": {
    "name": "The agent-router-cc Authors"
  }
}
