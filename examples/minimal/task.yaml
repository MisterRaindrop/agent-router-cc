# The task contract Opus authors (or you write by hand). No global policy: scope and
# the verify command live here. Copy to .router/tasks/slugify/task.yaml.
schema_version: 1
id: slugify
title: Implement slugify()
max_wall_minutes: 10
allowed_globs: ["src/**"]      # the executor may ONLY touch these paths
forbidden_globs: []
max_changed_lines: 100
verify: [["node", "--test"]]   # the mechanical gate (empty = diff/scope/secret only)
