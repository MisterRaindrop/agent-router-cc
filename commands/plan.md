---
description: Renamed -- use /router:workplan
---
This command is now **`/router:workplan`**. Run that instead; it takes the same arguments.

The old name is kept for one version so a machine that has not picked up the new plugin build
yet does not simply lose the command (see `DEPRECATIONS.md`). Nothing else lives here.
